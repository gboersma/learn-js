function size() {
    var length = parseInt(prompt("what is the length? "));
    var width = parseInt(prompt("What is the width? "));
    var perimeter = 2 * length + 2 * width;                                                          ;
    var area = length * width;
    alert("The area is: " + area + " The perimeter is: " + perimeter);
}
